# 新功能规格（精简版）：意大利语与剪贴板自动清理

## 背景与边界
- 产品定位：仅做“剪贴板清理”。不做历史列表、收藏、主题、统计等超出范围的功能。
- 本次新增：
  1) 新增意大利语（it）界面文案。
  2) 在 popup 增加“设置”，可配置自动清理剪贴板时间间隔（关闭/1 分钟/5 分钟）。

## 目标
- 全界面支持 it（与现有 en/zh 一致）。
- 用户可在 popup 设置中选择自动清理间隔；若开启，在到达间隔时自动清理系统剪贴板内容。

## 范围
- 包含：i18n 接入与 it 文案、popup 设置入口与单一选项、定时器触发的剪贴板清理逻辑、必要的最小存储。
- 不包含：历史记录、收藏/置顶、统计分析、跨设备同步、主题系统等。

## i18n 要求（最小实现）
- 语言包：提供 `en`、`zh`、`it` 三套 `messages.json`，key 统一。
- 默认语言：英文（en）。
- 用法：在 `popup.html`、`popup.js`、`bubble.js`、`background.js` 中将可见文案替换为 i18n key；无需实现“手动切换语言”的 UI，遵循 Chrome 默认语言。

## Popup 设置（最小 UI）
- 入口：在 `popup.html` 增加“设置”按钮或轻量入口区域。
- 内容：
  - 标题：设置
  - 选项：自动清理剪贴板
    - 可选值：关闭（默认）/ 1 分钟 / 5 分钟
  - 行为：选项变化即保存；无需额外确认按钮。

## 自动清理行为（仅剪贴板）
- 含义：当设置为 1 分钟或 5 分钟时，后台在达到所选间隔后执行一次“清空剪贴板”动作。
- 时机：
  - 浏览器启动或扩展加载时，根据当前设置初始化/更新定时器。
  - 用户修改设置后，立即重置定时器。
  - 到达间隔后执行一次清理，并继续按照相同间隔循环执行。
- 失败处理：清理失败（例如权限受限）仅静默忽略，不重试风暴；下个周期继续。

## 设置存储（最小）
- 存储介质：`chrome.storage.sync`。
- Key：`settings.autoClearClipboardMs`。
- 值域：`0`（关闭，默认）、`60000`、`300000`。
- 机制：popup 写入；background 监听 storage 变更或接收 runtime 消息以重置定时器。

## 权限
- 保持最小：`storage`；清理剪贴板沿用现有实现权限，无需新增高敏感权限。

## 隐私
- 所有操作均在本地完成；不上传任何内容。
- 无历史留存；清理即销毁。

## 文案示例 keys（最小集）
- settings.title：设置
- settings.autoClear.label：自动清理剪贴板
- settings.autoClear.off：关闭
- settings.autoClear.1m：1 分钟
- settings.autoClear.5m：5 分钟

## 验收标准（对齐 README“剪贴板清理”定位）
- 将系统语言设为意大利语时，界面显示 it 文案；无 key 时回退英文。
- popup 可见“自动清理剪贴板”选项；默认关闭。
- 选择 1 分钟或 5 分钟后，无需重启即可生效。
- 到达间隔后剪贴板内容被清空；随后继续按间隔循环清空。
- 关闭选项后不再自动清理。
- 重启浏览器后设置仍保持并按设定执行。

## 最小测试清单
- i18n：系统语言 it → 文案为 it；缺失 key → 回退 en。
- 设置存储：切换 Off/1m/5m，`chrome.storage.sync` 值正确。
- 定时器：设置 1m 后等待，剪贴板按期被清空；修改为 5m 后定时更新。
- 持久化：浏览器重启后，设置仍然生效。